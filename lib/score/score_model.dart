class PlayerScore {
  String playerName;
  int currentScore;
  int newCount = 0;
  int playerRank = 0;
  int dropSave = 0;
  bool isCompulsory = false;

  PlayerScore(this.playerName, this.currentScore);
}
